{
  "name": "qpp",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/index.js",
    "db:up": "docker compose --env-file ./backend/.env up -d mysql",
    "db:down": "docker compose --env-file ./backend/.env down",
    "db:logs": "docker compose logs -f mysql",
    "backend:docker": "docker compose --env-file ./backend/.env up -d backend",
    "backend:logs": "docker compose logs -f backend",
    "stack:up": "docker compose --env-file ./backend/.env up -d",
    "stack:down": "docker compose --env-file ./backend/.env down",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  }
}
